# Database Configuration
# For production, use a robust database like PostgreSQL or MySQL
# Example: postgresql://user:password@localhost:5432/mydb
DATABASE_URL="file:./dev.db"

# Next Auth Configuration
# The full URL of your deployment (e.g. https://your-app.com)
NEXTAUTH_URL="http://localhost:3000"

# Secret key for JWT encryption and session hashing
# Generate a strong random string: `openssl rand -base64 32`
NEXTAUTH_SECRET="changeme_in_prod_secret_key"

# Google OAuth Provider
# You will need to create a new OAuth client in Google Cloud Console
# authorized for your production domain.
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""
